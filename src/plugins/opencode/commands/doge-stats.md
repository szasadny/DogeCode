---
description: Show doge lifetime token-savings stats
---
Show doge stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/caveman/.caveman-history.jsonl`
(or wherever the doge-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
